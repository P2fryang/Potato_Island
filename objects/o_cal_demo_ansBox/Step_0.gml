
//TODO: update this with ids
if(global.selectedID != false){
	target = answers[? global.selectedID];
	
}


// TODO check if any white box object exist in room
// if no more exist, allow user to advance to next challenge

if(allow){
if(delay == -1){
	str = str;
}
else
if(string_length(keyboard_string)<=15){
	str = keyboard_string;
}
else{
	keyboard_string = str;
}
}
